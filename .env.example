# Serveur frontend (utilise par `npm run dev` et `npm start`)
PORT=3000
HOSTNAME=0.0.0.0

# Backend Nino, sans slash final
NEXT_PUBLIC_NINO_API_URL=http://localhost:8000

# Desactive la telemetrie Next.js (optionnel)
NEXT_TELEMETRY_DISABLED=1
